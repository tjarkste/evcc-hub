import { Configuration, Package } from "./interfaces";
export declare function getInstalledPackages(configuration: Pick<Configuration, "direct" | "development" | "production">, nodeModulesPath: string, packageJsonPath?: string): Promise<Array<Package>>;
