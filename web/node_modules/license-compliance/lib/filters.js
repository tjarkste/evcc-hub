"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.queryPackages = exports.excludePackages = void 0;
function excludePackages(packages, configuration) {
    const { exclude: excludeFilters } = configuration;
    if (excludeFilters.length === 0) {
        return packages;
    }
    return packages.filter((pack) => {
        for (const filter of excludeFilters) {
            if (typeof filter === "string") {
                if (pack.name === filter) {
                    return false;
                }
            }
            else if (filter.test(pack.name)) {
                return false;
            }
        }
        return true;
    });
}
exports.excludePackages = excludePackages;
function queryPackages(packages, configuration) {
    const { query: queryFilter } = configuration;
    if (queryFilter.length === 0) {
        return packages;
    }
    return packages.filter((value) => queryFilter.includes(value.license));
}
exports.queryPackages = queryPackages;
//# sourceMappingURL=filters.js.map