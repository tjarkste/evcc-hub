import { Package } from "../interfaces";
import { Formatter } from "./formatter";
export declare class Csv implements Formatter {
    detail(packages: Array<Package>): void;
    summary(licenses: Array<{
        name: string;
        count: number;
    }>): void;
}
