"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Csv = void 0;
const os_1 = require("os");
const enumerations_1 = require("../enumerations");
class Csv {
    detail(packages) {
        let buffer = `"package name","version","license","license file","repository"${os_1.EOL}`;
        for (const pack of packages) {
            buffer += `"${pack.name}","${pack.version}","${pack.license}","${pack.licenseFile ? pack.licenseFile : enumerations_1.Literals.UNKNOWN}","${pack.repository}"${os_1.EOL}`;
        }
        console.info(buffer);
    }
    summary(licenses) {
        let buffer = `"license","count"${os_1.EOL}`;
        for (const license of licenses) {
            buffer += `"${license.name}","${license.count}"${os_1.EOL}`;
        }
        console.info(buffer);
    }
}
exports.Csv = Csv;
//# sourceMappingURL=csv.js.map