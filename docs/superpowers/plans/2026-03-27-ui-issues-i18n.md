# UI Issues & i18n Migration — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix four UI issues (duplicate spinner, blocked settings navigation, irrelevant help links, mixed-language UI) and migrate all hub-specific strings to i18n.

**Architecture:** Three independent structural fixes (Tasks 1-3) can be done in any order. Task 4 adds all translation keys as a foundation — it must complete before Tasks 5-12. Tasks 5-12 (per-component i18n) are independent of each other but all depend on Task 4.

**Tech Stack:** Vue 3, TypeScript, Vue i18n, Vitest, @vue/test-utils, Bootstrap 5, Vue Router

**Spec:** `docs/superpowers/specs/2026-03-26-ui-issues-i18n-design.md`

**Working directory for all commands:** `/Users/tjarksteenblock/Documents/GitHub/evcc_hub/web`

---

## File Map

| File | Change |
|---|---|
| `assets/js/components/ConnectionStatus.vue` | Remove spinner element; add $t() for status strings |
| `assets/js/router.ts` | Add `meta: { noDataRequired: true }` to /overview and /settings |
| `assets/js/views/App.vue` | Update `waitingForData` computed to use noDataRequired meta; add $t() for loading text |
| `assets/js/components/HelpModal.vue` | Remove logs/issue/restart sections; add hub GitHub link; remove unused imports |
| `assets/js/components/Top/Navigation.vue` | Remove evcc.io link; add $t() for hardcoded strings |
| `web/i18n/en.json` | Add `hub.*` namespace with all English translations |
| `web/i18n/de.json` | Add `hub.*` namespace with all German translations |
| `assets/js/components/WaitingForData.vue` | Add $t() for all strings |
| `assets/js/components/WaitingForData.test.ts` | Update assertions to use i18n key strings |
| `assets/js/components/ErrorBoundary.vue` | Add $t() for error text and retry button |
| `assets/js/views/LoginView.vue` | Add $t() for all strings |
| `assets/js/views/SettingsView.vue` | Add $t() for all strings; fix hardcoded `de-DE` locale |
| `assets/js/views/SiteManager.vue` | Add $t() for all strings |
| `assets/js/views/SiteOverview.vue` | Add $t() for all strings |
| `assets/js/components/SiteCredentialsModal.vue` | Add $t() for all strings |
| `assets/js/views/Datenschutz.vue` | Refactor v-html loading state to template; add $t() |
| `assets/js/views/Impressum.vue` | Refactor v-html loading state to template; add $t() |
| `assets/js/views/Nutzungsbedingungen.vue` | Refactor v-html loading state to template; add $t() |
| `assets/js/views/Optimize.vue` | Add $t() for debug page strings |
| `assets/js/views/Energy.vue` | Add $t() for WIP page strings |
| `assets/js/views/Issue.vue` | Add $t() for form strings and error messages |
| `assets/js/components/Optimize/BatteryConfigurationTable.vue` | Add $t() for all table headers and value labels |
| `assets/js/components/Energyflow/Visualization.vue` | Add $t() for "In" / "Out" labels |
| `assets/js/components/Config/AuthSuccessBanner.vue` | **Deferred** — `"Unknown"` is an edge-case dev fallback, not user-facing in normal flows. No task created. |

---

## Task 1: Remove Spinner from ConnectionStatus

**Files:**
- Modify: `assets/js/components/ConnectionStatus.vue:10-15`

- [ ] **Step 1: Delete the spinner span**

In `ConnectionStatus.vue`, delete lines 10–15 (the `<span v-if="isReconnecting" class="spinner-border spinner-border-sm">` element):

```html
<!-- DELETE these 6 lines: -->
<span
  v-if="isReconnecting"
  class="spinner-border spinner-border-sm"
  role="status"
  aria-hidden="true"
></span>
```

The text `{{ statusText }}` on the next line stays. The `isReconnecting` computed property also stays (still used for `statusClass`).

- [ ] **Step 2: Run tests**

```bash
npm run test -- --run
```
Expected: all tests pass

- [ ] **Step 3: Commit**

```bash
git add assets/js/components/ConnectionStatus.vue
git commit -m "fix: remove duplicate spinner from ConnectionStatus"
```

---

## Task 2: Add noDataRequired Route Meta

**Files:**
- Modify: `assets/js/router.ts:75,136-138`
- Modify: `assets/js/views/App.vue:146-151`

- [ ] **Step 1: Update /overview route in router.ts**

Change line 75 from:
```ts
{ path: "/overview", component: SiteOverview },
```
to:
```ts
{ path: "/overview", component: SiteOverview, meta: { noDataRequired: true } },
```

- [ ] **Step 2: Update /settings route in router.ts**

Change lines 136–138 from:
```ts
{
  path: "/settings",
  component: () => import("./views/SettingsView.vue"),
},
```
to:
```ts
{
  path: "/settings",
  component: () => import("./views/SettingsView.vue"),
  meta: { noDataRequired: true },
},
```

- [ ] **Step 3: Update waitingForData computed in App.vue**

Change lines 146–151 from:
```js
waitingForData(): boolean {
  if (this.hasCachedState) return false;
  if (this.$route.meta["noAuth"]) return false;
  if (this.$route.path === '/overview') return false;
  return store.state.lastDataAt === null;
},
```
to:
```js
waitingForData(): boolean {
  if (this.hasCachedState) return false;
  if (this.$route.meta["noAuth"]) return false;
  if (this.$route.meta["noDataRequired"]) return false;
  return store.state.lastDataAt === null;
},
```

- [ ] **Step 4: Run tests**

```bash
npm run test -- --run && npm run lint:tsc
```
Expected: all tests pass, no TypeScript errors

- [ ] **Step 5: Commit**

```bash
git add assets/js/router.ts assets/js/views/App.vue
git commit -m "feat: add noDataRequired route meta, fix settings page access"
```

---

## Task 3: Slim HelpModal + Remove evcc.io Link

**Files:**
- Modify: `assets/js/components/HelpModal.vue`
- Modify: `assets/js/components/Top/Navigation.vue`

- [ ] **Step 1: Remove evcc.io link from Navigation.vue**

Delete lines 123–131 (the entire `<li>` containing the evcc.io anchor):
```html
<li>
  <a class="dropdown-item d-flex" href="https://evcc.io/" target="_blank">
    <span>evcc.io</span>
    <shopicon-regular-newtab
      size="s"
      class="ms-2 external"
    ></shopicon-regular-newtab>
  </a>
</li>
```

Also remove:
- Line 157 in `<script>`: `import "@h2d2/shopicons/es/regular/newtab";`
- In `<style scoped>` (around lines 283–286): remove only the `.external { width: 18px; height: 20px; }` block — keep `.menu-button` and `.dropdown-menu` style rules which are unrelated.

- [ ] **Step 2: Rewrite HelpModal.vue**

Replace the entire file with:

```vue
<template>
	<Teleport to="body">
		<div
			id="helpModal"
			class="modal fade text-dark"
			tabindex="-1"
			role="dialog"
			aria-hidden="true"
		>
			<div class="modal-dialog modal-dialog-centered" role="document">
				<div class="modal-content">
					<div class="modal-header">
						<h5 class="modal-title">{{ $t("help.modalTitle") }}</h5>
						<button
							type="button"
							class="btn-close"
							data-bs-dismiss="modal"
							aria-label="Close"
						></button>
					</div>
					<div class="modal-body">
						<p>
							{{ $t("help.primaryActions") }}
						</p>
						<div
							class="d-block d-sm-flex justify-content-between align-items-stretch mb-4"
						>
							<a
								:href="docsUrl"
								target="_blank"
								class="btn btn-outline-primary w-100 w-sm-auto flex-grow-1 mb-3 mb-sm-0 me-sm-3"
								type="button"
							>
								{{ $t("help.documentationButton") }}
							</a>
							<a
								href="https://github.com/evcc-io/evcc/discussions"
								target="_blank"
								class="btn btn-outline-primary w-100 w-sm-auto flex-grow-1 mb-3 mb-sm-0 me-sm-3"
								type="button"
							>
								{{ $t("help.discussionsButton") }}
							</a>
							<a
								href="https://github.com/tjarkste/evcc-hub"
								target="_blank"
								class="btn btn-outline-secondary w-100 w-sm-auto flex-grow-1"
								type="button"
							>
								{{ $t("hub.help.hubGithub") }}
							</a>
						</div>
					</div>
				</div>
			</div>
		</div>
	</Teleport>
</template>

<script lang="ts">
import { docsPrefix } from "../i18n";
import { defineComponent } from "vue";

export default defineComponent({
	name: "HelpModal",
	computed: {
		docsUrl() {
			return `${docsPrefix()}/`;
		},
	},
});
</script>
```

- [ ] **Step 3: Run tests**

```bash
npm run test -- --run && npm run lint:tsc
```
Expected: all tests pass, no TypeScript errors

- [ ] **Step 4: Commit**

```bash
git add assets/js/components/HelpModal.vue assets/js/components/Top/Navigation.vue
git commit -m "feat: slim HelpModal to docs/discussions/hub-github, remove evcc.io link"
```

---

## Task 4: Add hub.* Translation Keys to en.json and de.json

**Files:**
- Modify: `i18n/en.json`
- Modify: `i18n/de.json`

This is a prerequisite for all i18n tasks (5–12). Add a new top-level `"hub"` key after `"help"` (alphabetical order).

> Note: `hub.help.hubGithub` is already used in the HelpModal written in Task 3 — if Task 3 ran first, the app will show the key as fallback until this task completes. That is fine for development order.

- [ ] **Step 1: Add hub section to en.json**

Insert this entire block after the `"help": { ... }` section in `i18n/en.json`:

```json
"hub": {
  "auth": {
    "alreadyHaveAccount": "Already have an account? Sign in",
    "createFreeAccount": "Create a free account",
    "emailPlaceholder": "Email",
    "loginButton": "Sign In",
    "loginFailed": "Login failed. Please check your email and password.",
    "loggingIn": "Signing in...",
    "noAccount": "No account yet? Register",
    "onboarding": {
      "copied": "Copied!",
      "copy": "Copy",
      "step1Hint": "If not yet:",
      "step1Link": "Install evcc →",
      "step1Title": "evcc installed?",
      "step2Hint": "Add these lines to your evcc.yaml:",
      "step2Title": "Add MQTT configuration",
      "step3Title": "Restart evcc",
      "subtitle": "Now connect your evcc instance.",
      "toDashboard": "Go to Dashboard",
      "title": "Account created!"
    },
    "openSource": "Open Source on GitHub →",
    "passwordPlaceholder": "Password",
    "registerButton": "Register",
    "registerFailed": "Registration failed. Please try again.",
    "registering": "Registering...",
    "tagline": "Your evcc dashboard, accessible from anywhere."
  },
  "connection": {
    "offline": "Offline",
    "reconnecting": "Reconnecting...",
    "staleMinutes": "(last update {minutes}m ago)",
    "staleSeconds": "(last update {seconds}s ago)"
  },
  "debug": {
    "battery": {
      "battery": "Battery",
      "charge": "Charge",
      "chargeDischarge": "Charge / Discharge",
      "demandProfile": "Demand Profile",
      "discharge": "Discharge",
      "energyValue": "Energy Value",
      "goals": "goals",
      "gridInteraction": "Grid Interaction",
      "maxDischarge": "Max Discharge",
      "none": "None",
      "powerRange": "Power Range",
      "socGoals": "SoC Goals",
      "socRange": "SoC Range",
      "stateOfCharge": "State of Charge",
      "steps": "steps"
    },
    "energy": {
      "nothingToSee": "nothing to see here",
      "title": "Energy Overview",
      "wip": "This page is work in progress."
    },
    "optimize": {
      "batteryInput": "Input: Battery",
      "chargeEfficiency": "charge efficiency",
      "chargingPlanResult": "Result: Charging Plan",
      "description": "This page is for development purposes only. Gives insights into the upcoming optimization algorithm.",
      "dischargeEfficiency": "discharge efficiency",
      "gridPricesInput": "Input: Grid Prices",
      "nothingToSee": "nothing to see here",
      "rawData": "Raw Data",
      "request": "Request:",
      "response": "Response:",
      "saved": "saved",
      "socProjectionResult": "Result: SoC Projection",
      "timeSeries": "Time Series",
      "title": "Optimize Debug"
    }
  },
  "energyflow": {
    "in": "In",
    "out": "Out"
  },
  "error": {
    "failedToRender": "failed to render.",
    "retry": "Retry",
    "unknown": "Unknown error"
  },
  "help": {
    "hubGithub": "evcc Hub on GitHub"
  },
  "issue": {
    "loadApiConfigError": "Failed to load API configuration",
    "loadConfigError": "Failed to load configuration",
    "loadLogsError": "Failed to load logs",
    "loadStateError": "Failed to load system state",
    "englishNote": "Please write your issue in English so everyone can participate.",
    "titlePlaceholder": "Brief description of the problem"
  },
  "legal": {
    "loading": "Loading..."
  },
  "nav": {
    "optimize": "Optimize 🧪",
    "profileSettings": "Profile & Settings"
  },
  "settings": {
    "accountSection": "Account",
    "changePassword": "Change Password",
    "confirmPassword": "Confirm New Password",
    "currentPassword": "Current Password",
    "emailLabel": "Email address",
    "loading": "Loading...",
    "logout": "Log out",
    "newPassword": "New Password",
    "passwordChangeError": "Could not change password.",
    "passwordMismatch": "The new passwords do not match.",
    "passwordSuccess": "Password changed successfully. You will be logged out shortly...",
    "profileLoadError": "Could not load profile.",
    "registeredSince": "Registered since",
    "savePassword": "Save Password",
    "saving": "Saving…",
    "title": "Profile & Settings"
  },
  "sites": {
    "active": "Active",
    "add": "Add",
    "addSite": "Add New Site",
    "adding": "Adding...",
    "backToDashboard": "Back to Dashboard",
    "copy": "Copy",
    "copied": "Copied!",
    "createError": "Could not create site.",
    "credentials": {
      "brokerPort": "Broker Port",
      "brokerUrl": "Broker URL",
      "close": "Close",
      "copied": "Copied!",
      "copy": "Copy",
      "description": "You need these credentials for the evcc configuration of this site.",
      "hide": "Hide",
      "loadError": "Could not load credentials.",
      "loading": "Loading…",
      "mqttPassword": "MQTT Password",
      "mqttUsername": "MQTT Username",
      "show": "Show",
      "title": "MQTT Credentials — {siteName}",
      "topicPrefix": "Topic Prefix"
    },
    "delete": "Delete",
    "deleteError": "Could not delete site.",
    "loadError": "Could not load sites.",
    "manageSites": "Manage Sites",
    "mqttConfigHint": "Add these lines to your evcc.yaml:",
    "namePlaceholder": "Name (e.g. Holiday Home)",
    "select": "Select",
    "siteCreatedSuffix": "was created.",
    "title": "My Sites",
    "view": "View"
  },
  "waiting": {
    "connecting": "Connecting to MQTT broker...",
    "loading": "Loading...",
    "noData": "No data received.",
    "noDataHint": "Is your evcc instance online and configured correctly?",
    "waitingForData": "Connected — waiting for data from your evcc instance..."
  }
}
```

- [ ] **Step 2: Add hub section to de.json**

Insert this entire block after the `"help": { ... }` section in `i18n/de.json`:

```json
"hub": {
  "auth": {
    "alreadyHaveAccount": "Bereits ein Konto? Anmelden",
    "createFreeAccount": "Kostenlosen Account erstellen",
    "emailPlaceholder": "E-Mail",
    "loginButton": "Anmelden",
    "loginFailed": "Anmeldung fehlgeschlagen. Bitte E-Mail und Passwort prüfen.",
    "loggingIn": "Wird angemeldet...",
    "noAccount": "Noch kein Konto? Registrieren",
    "onboarding": {
      "copied": "Kopiert!",
      "copy": "Kopieren",
      "step1Hint": "Falls noch nicht:",
      "step1Link": "evcc installieren →",
      "step1Title": "evcc installiert?",
      "step2Hint": "Füge diese Zeilen in deine evcc.yaml ein:",
      "step2Title": "MQTT-Konfiguration hinzufügen",
      "step3Title": "evcc neu starten",
      "subtitle": "Verbinde jetzt deine evcc-Instanz.",
      "toDashboard": "Weiter zum Dashboard",
      "title": "Konto erstellt!"
    },
    "openSource": "Open Source auf GitHub →",
    "passwordPlaceholder": "Passwort",
    "registerButton": "Registrieren",
    "registerFailed": "Registrierung fehlgeschlagen. Bitte versuche es erneut.",
    "registering": "Wird registriert...",
    "tagline": "Dein evcc-Dashboard, von überall erreichbar."
  },
  "connection": {
    "offline": "Offline",
    "reconnecting": "Verbinde...",
    "staleMinutes": "(zuletzt aktualisiert vor {minutes}m)",
    "staleSeconds": "(zuletzt aktualisiert vor {seconds}s)"
  },
  "debug": {
    "battery": {
      "battery": "Batterie",
      "charge": "Laden",
      "chargeDischarge": "Laden / Entladen",
      "demandProfile": "Lastprofil",
      "discharge": "Entladen",
      "energyValue": "Energiewert",
      "goals": "Ziele",
      "gridInteraction": "Netzinteraktion",
      "maxDischarge": "Max. Entladung",
      "none": "Keine",
      "powerRange": "Leistungsbereich",
      "socGoals": "SoC-Ziele",
      "socRange": "SoC-Bereich",
      "stateOfCharge": "Ladestand",
      "steps": "Schritte"
    },
    "energy": {
      "nothingToSee": "nichts zu sehen",
      "title": "Energie-Übersicht",
      "wip": "Diese Seite befindet sich in Arbeit."
    },
    "optimize": {
      "batteryInput": "Eingabe: Batterie",
      "chargeEfficiency": "Ladeeffizienz",
      "chargingPlanResult": "Ergebnis: Ladeplan",
      "description": "Diese Seite ist nur für Entwicklungszwecke. Gibt Einblicke in den kommenden Optimierungsalgorithmus.",
      "dischargeEfficiency": "Entladeeffizienz",
      "gridPricesInput": "Eingabe: Netzpreise",
      "nothingToSee": "nichts zu sehen",
      "rawData": "Rohdaten",
      "request": "Anfrage:",
      "response": "Antwort:",
      "saved": "gespart",
      "socProjectionResult": "Ergebnis: SoC-Projektion",
      "timeSeries": "Zeitreihe",
      "title": "Optimize Debug"
    }
  },
  "energyflow": {
    "in": "Einspeisung",
    "out": "Bezug"
  },
  "error": {
    "failedToRender": "failed to render.",
    "retry": "Wiederholen",
    "unknown": "Unbekannter Fehler"
  },
  "help": {
    "hubGithub": "evcc Hub auf GitHub"
  },
  "issue": {
    "loadApiConfigError": "API-Konfiguration konnte nicht geladen werden",
    "loadConfigError": "Konfiguration konnte nicht geladen werden",
    "loadLogsError": "Logs konnten nicht geladen werden",
    "loadStateError": "Systemstatus konnte nicht geladen werden",
    "englishNote": "Bitte schreibe dein Issue auf Englisch, damit alle mitmachen können.",
    "titlePlaceholder": "Kurze Beschreibung des Problems"
  },
  "legal": {
    "loading": "Lädt..."
  },
  "nav": {
    "optimize": "Optimize 🧪",
    "profileSettings": "Profil & Einstellungen"
  },
  "settings": {
    "accountSection": "Konto",
    "changePassword": "Passwort ändern",
    "confirmPassword": "Neues Passwort bestätigen",
    "currentPassword": "Aktuelles Passwort",
    "emailLabel": "E-Mail-Adresse",
    "loading": "Lädt...",
    "logout": "Abmelden",
    "newPassword": "Neues Passwort",
    "passwordChangeError": "Passwort konnte nicht geändert werden.",
    "passwordMismatch": "Die neuen Passwörter stimmen nicht überein.",
    "passwordSuccess": "Passwort erfolgreich geändert. Du wirst in Kürze abgemeldet…",
    "profileLoadError": "Profil konnte nicht geladen werden.",
    "registeredSince": "Registriert seit",
    "savePassword": "Passwort speichern",
    "saving": "Speichern…",
    "title": "Profil & Einstellungen"
  },
  "sites": {
    "active": "Aktiv",
    "add": "Hinzufügen",
    "addSite": "Neuen Standort hinzufügen",
    "adding": "...",
    "backToDashboard": "Zurück zum Dashboard",
    "copy": "Kopieren",
    "copied": "Kopiert!",
    "createError": "Standort konnte nicht erstellt werden.",
    "credentials": {
      "brokerPort": "Broker Port",
      "brokerUrl": "Broker URL",
      "close": "Schließen",
      "copied": "Kopiert!",
      "copy": "Kopieren",
      "description": "Diese Daten brauchst du für die evcc-Konfiguration dieser Site.",
      "hide": "Verbergen",
      "loadError": "Zugangsdaten konnten nicht geladen werden.",
      "loading": "Lädt…",
      "mqttPassword": "MQTT-Passwort",
      "mqttUsername": "MQTT-Benutzername",
      "show": "Anzeigen",
      "title": "MQTT-Zugangsdaten — {siteName}",
      "topicPrefix": "Topic Prefix"
    },
    "delete": "Löschen",
    "deleteError": "Standort konnte nicht gelöscht werden.",
    "loadError": "Standorte konnten nicht geladen werden.",
    "manageSites": "Standorte verwalten",
    "mqttConfigHint": "Füge diese Zeilen in deine evcc.yaml ein:",
    "namePlaceholder": "Name (z.B. Ferienhaus)",
    "select": "Auswählen",
    "siteCreatedSuffix": "wurde erstellt.",
    "title": "Meine Standorte",
    "view": "Anzeigen"
  },
  "waiting": {
    "connecting": "Verbinde mit MQTT-Broker...",
    "loading": "Laden...",
    "noData": "Keine Daten empfangen.",
    "noDataHint": "Ist deine evcc-Instanz online und korrekt konfiguriert?",
    "waitingForData": "Verbunden — warte auf Daten von deiner evcc-Instanz..."
  }
}
```

- [ ] **Step 3: Run i18n check**

```bash
npm run lint:i18n
```
Expected: `✅ All translations have correct placeholders!`

- [ ] **Step 4: Commit**

```bash
git add i18n/en.json i18n/de.json
git commit -m "feat: add hub.* i18n namespace with DE and EN translations"
```

---

## Task 5: i18n WaitingForData.vue + Update Test

**Files:**
- Modify: `assets/js/components/WaitingForData.vue`
- Modify: `assets/js/components/WaitingForData.test.ts`

- [ ] **Step 1: Update test to use i18n keys**

Replace the entire `WaitingForData.test.ts` with:

```ts
import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";
import { mount } from "@vue/test-utils";
import WaitingForData from "./WaitingForData.vue";
import store from "../store";
import { ConnectionState } from "../types/evcc";

const mockT = (key: string) => key;

describe("WaitingForData", () => {
  beforeEach(() => {
    vi.useFakeTimers();
    store.state.connectionState = ConnectionState.OFFLINE;
    store.state.lastDataAt = null;
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  it("shows connecting key for RECONNECTING state", () => {
    store.state.connectionState = ConnectionState.RECONNECTING;
    const wrapper = mount(WaitingForData, { global: { mocks: { $t: mockT } } });
    expect(wrapper.text()).toContain("hub.waiting.connecting");
  });

  it("shows connecting key for OFFLINE state", () => {
    store.state.connectionState = ConnectionState.OFFLINE;
    const wrapper = mount(WaitingForData, { global: { mocks: { $t: mockT } } });
    expect(wrapper.text()).toContain("hub.waiting.connecting");
  });

  it("shows waitingForData key when CONNECTED", () => {
    store.state.connectionState = ConnectionState.CONNECTED;
    const wrapper = mount(WaitingForData, { global: { mocks: { $t: mockT } } });
    expect(wrapper.text()).toContain("hub.waiting.waitingForData");
  });
});
```

- [ ] **Step 2: Run test to confirm it fails**

```bash
npm run test -- --run WaitingForData
```
Expected: FAIL — the component still renders hardcoded strings (not i18n keys), so the assertions checking for `"hub.waiting.connecting"` etc. will not match.

> **Note on test environment:** The global Vitest setup (`assets/js/test-setup.ts`) does NOT provide a global `$t` mock. The current component uses hardcoded strings so there is no `$t` issue yet. After migration in Step 3, calling `this.$t(...)` without the mock would throw `TypeError: this.$t is not a function` — which is also a FAIL. The new test in Step 1 resolves this by providing `$t` via `global: { mocks: { $t: mockT } }`.

- [ ] **Step 3: Migrate WaitingForData.vue**

In the template:
- Replace `Keine Daten empfangen.` with `{{ $t('hub.waiting.noData') }}`
- Replace `Ist deine evcc-Instanz online und korrekt konfiguriert?` with `{{ $t('hub.waiting.noDataHint') }}`
- Replace `Laden...` (the visually-hidden span) with `{{ $t('hub.waiting.loading') }}`

In the `statusTitle()` computed:
- Replace `"Verbinde mit MQTT-Broker..."` (both occurrences) with `this.$t('hub.waiting.connecting')`
- Replace `"Verbunden — warte auf Daten von deiner evcc-Instanz..."` with `this.$t('hub.waiting.waitingForData')`

- [ ] **Step 4: Run test to confirm pass**

```bash
npm run test -- --run WaitingForData
```
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add assets/js/components/WaitingForData.vue assets/js/components/WaitingForData.test.ts
git commit -m "feat: i18n WaitingForData component"
```

---

## Task 6: i18n ConnectionStatus.vue + App.vue

**Files:**
- Modify: `assets/js/components/ConnectionStatus.vue`
- Modify: `assets/js/views/App.vue`

- [ ] **Step 1: Update ConnectionStatus.vue statusText computed**

Replace:
```js
statusText(): string {
  switch (this.connectionState) {
    case ConnectionState.RECONNECTING:
      return 'Reconnecting...'
    case ConnectionState.OFFLINE:
      return 'Offline'
    default:
      return ''
  }
},
```
With:
```js
statusText(): string {
  switch (this.connectionState) {
    case ConnectionState.RECONNECTING:
      return this.$t('hub.connection.reconnecting')
    case ConnectionState.OFFLINE:
      return this.$t('hub.connection.offline')
    default:
      return ''
  }
},
```

- [ ] **Step 2: Update ConnectionStatus.vue staleText computed**

Replace:
```js
if (seconds < 120) return `(last update ${seconds}s ago)`
const minutes = Math.round(seconds / 60)
return `(last update ${minutes}m ago)`
```
With:
```js
if (seconds < 120) return this.$t('hub.connection.staleSeconds', { seconds })
const minutes = Math.round(seconds / 60)
return this.$t('hub.connection.staleMinutes', { minutes })
```

- [ ] **Step 3: Update App.vue loading spinner text**

Replace:
```html
<span class="visually-hidden">Laden...</span>
```
With:
```html
<span class="visually-hidden">{{ $t('hub.waiting.loading') }}</span>
```

- [ ] **Step 4: Run tests**

```bash
npm run test -- --run && npm run lint:tsc
```
Expected: all tests pass, no TypeScript errors

- [ ] **Step 5: Commit**

```bash
git add assets/js/components/ConnectionStatus.vue assets/js/views/App.vue
git commit -m "feat: i18n ConnectionStatus and App loading text"
```

---

## Task 7: i18n Navigation.vue + ErrorBoundary.vue

**Files:**
- Modify: `assets/js/components/Top/Navigation.vue`
- Modify: `assets/js/components/ErrorBoundary.vue`

- [ ] **Step 1: Update Navigation.vue**

Line 68: Replace `Profil & Einstellungen` with `{{ $t('hub.nav.profileSettings') }}`

Line 95: Replace `Optimize 🧪` with `{{ $t('hub.nav.optimize') }}`

- [ ] **Step 2: Update ErrorBoundary.vue template**

Replace:
```html
<strong>{{ section }} failed to render.</strong>
<p class="mb-1 small">{{ errorMessage }}</p>
<button class="btn btn-sm btn-outline-secondary" @click="reset">Retry</button>
```
With:
```html
<strong>{{ section }} {{ $t('hub.error.failedToRender') }}</strong>
<p class="mb-1 small">{{ errorMessage }}</p>
<button class="btn btn-sm btn-outline-secondary" @click="reset">{{ $t('hub.error.retry') }}</button>
```

- [ ] **Step 3: Update ErrorBoundary.vue errorCaptured hook**

Replace:
```js
this.errorMessage = err.message || 'Unknown error'
```
With:
```js
this.errorMessage = err.message || this.$t('hub.error.unknown')
```

- [ ] **Step 4: Run tests**

```bash
npm run test -- --run && npm run lint:tsc
```
Expected: all tests pass

- [ ] **Step 5: Commit**

```bash
git add assets/js/components/Top/Navigation.vue assets/js/components/ErrorBoundary.vue
git commit -m "feat: i18n Navigation and ErrorBoundary"
```

---

## Task 8: i18n LoginView.vue

**Files:**
- Modify: `assets/js/views/LoginView.vue`

- [ ] **Step 1: Update template strings**

Apply the following replacements in the template section:

| Old | New |
|---|---|
| `Dein evcc-Dashboard, von überall erreichbar.` | `{{ $t('hub.auth.tagline') }}` |
| `Open Source auf GitHub →` | `{{ $t('hub.auth.openSource') }}` |
| `placeholder="E-Mail"` | `:placeholder="$t('hub.auth.emailPlaceholder')"` |
| `placeholder="Passwort"` | `:placeholder="$t('hub.auth.passwordPlaceholder')"` |
| `{{ loading ? 'Wird angemeldet...' : 'Anmelden' }}` | `{{ loading ? $t('hub.auth.loggingIn') : $t('hub.auth.loginButton') }}` |
| `Noch kein Konto? Registrieren` | `{{ $t('hub.auth.noAccount') }}` |
| `Kostenlosen Account erstellen` | `{{ $t('hub.auth.createFreeAccount') }}` |
| `{{ loading ? 'Wird registriert...' : 'Registrieren' }}` | `{{ loading ? $t('hub.auth.registering') : $t('hub.auth.registerButton') }}` |
| `Bereits ein Konto? Anmelden` | `{{ $t('hub.auth.alreadyHaveAccount') }}` |
| `Konto erstellt!` (h2) | `{{ $t('hub.auth.onboarding.title') }}` |
| `Verbinde jetzt deine evcc-Instanz.` | `{{ $t('hub.auth.onboarding.subtitle') }}` |
| `evcc installiert?` | `{{ $t('hub.auth.onboarding.step1Title') }}` |
| `Falls noch nicht:` | `{{ $t('hub.auth.onboarding.step1Hint') }}` |
| `evcc installieren →` | `{{ $t('hub.auth.onboarding.step1Link') }}` |
| `MQTT-Konfiguration hinzufügen` | `{{ $t('hub.auth.onboarding.step2Title') }}` |
| `Füge diese Zeilen in deine evcc.yaml ein:` | `{{ $t('hub.auth.onboarding.step2Hint') }}` |
| `evcc neu starten` | `{{ $t('hub.auth.onboarding.step3Title') }}` |
| `{{ copied ? '✓ Kopiert!' : 'Kopieren' }}` | `{{ copied ? $t('hub.auth.onboarding.copied') : $t('hub.auth.onboarding.copy') }}` |
| `Weiter zum Dashboard` | `{{ $t('hub.auth.onboarding.toDashboard') }}` |

- [ ] **Step 2: Update script error messages**

Replace:
```js
this.error = 'Anmeldung fehlgeschlagen. Bitte E-Mail und Passwort prüfen.'
```
With:
```js
this.error = this.$t('hub.auth.loginFailed')
```

Replace:
```js
this.error = 'Registrierung fehlgeschlagen. Bitte versuche es erneut.'
```
With:
```js
this.error = this.$t('hub.auth.registerFailed')
```

- [ ] **Step 3: Run tests**

```bash
npm run test -- --run && npm run lint:tsc
```
Expected: all tests pass

- [ ] **Step 4: Commit**

```bash
git add assets/js/views/LoginView.vue
git commit -m "feat: i18n LoginView"
```

---

## Task 9: i18n SettingsView.vue

**Files:**
- Modify: `assets/js/views/SettingsView.vue`

- [ ] **Step 1: Update template strings**

Apply the following replacements:

| Old | New |
|---|---|
| `<h2 class="mb-4">Profil & Einstellungen</h2>` | `<h2 class="mb-4">{{ $t('hub.settings.title') }}</h2>` |
| `<h5 class="mb-0">Konto</h5>` | `<h5 class="mb-0">{{ $t('hub.settings.accountSection') }}</h5>` |
| `Lädt...` (profileLoading div text) | `{{ $t('hub.settings.loading') }}` |
| `E-Mail-Adresse` (label) | `{{ $t('hub.settings.emailLabel') }}` |
| `Registriert seit` (label) | `{{ $t('hub.settings.registeredSince') }}` |
| `<h5 class="mb-0">Passwort ändern</h5>` | `<h5 class="mb-0">{{ $t('hub.settings.changePassword') }}</h5>` |
| `Passwort erfolgreich geändert. Du wirst in Kürze abgemeldet…` | `{{ $t('hub.settings.passwordSuccess') }}` |
| `Aktuelles Passwort` (label) | `{{ $t('hub.settings.currentPassword') }}` |
| `Neues Passwort` (label, first occurrence) | `{{ $t('hub.settings.newPassword') }}` |
| `Neues Passwort bestätigen` (label) | `{{ $t('hub.settings.confirmPassword') }}` |
| `{{ passwordSaving ? 'Speichern…' : 'Passwort speichern' }}` | `{{ passwordSaving ? $t('hub.settings.saving') : $t('hub.settings.savePassword') }}` |
| `Abmelden` | `{{ $t('hub.settings.logout') }}` |

- [ ] **Step 2: Update script error messages**

Replace:
```js
if (!resp.ok) throw new Error('Profil konnte nicht geladen werden.')
```
With:
```js
if (!resp.ok) throw new Error(this.$t('hub.settings.profileLoadError'))
```

Replace (in catch block):
```js
this.profileError = err.message || 'Profil konnte nicht geladen werden.'
```
With:
```js
this.profileError = err.message || this.$t('hub.settings.profileLoadError')
```

Replace:
```js
this.passwordError = 'Die neuen Passwörter stimmen nicht überein.'
```
With:
```js
this.passwordError = this.$t('hub.settings.passwordMismatch')
```

Replace:
```js
throw new Error(data.error || 'Passwort konnte nicht geändert werden.')
```
With:
```js
throw new Error(data.error || this.$t('hub.settings.passwordChangeError'))
```

Replace (in catch block):
```js
this.passwordError = err.message || 'Passwort konnte nicht geändert werden.'
```
With:
```js
this.passwordError = err.message || this.$t('hub.settings.passwordChangeError')
```

- [ ] **Step 3: Fix hardcoded de-DE locale**

Replace:
```js
return new Date(this.profile.createdAt).toLocaleDateString('de-DE', {
```
With:
```js
return new Date(this.profile.createdAt).toLocaleDateString(this.$i18n.locale, {
```

- [ ] **Step 4: Run tests**

```bash
npm run test -- --run && npm run lint:tsc
```
Expected: all tests pass, no TypeScript errors

- [ ] **Step 5: Commit**

```bash
git add assets/js/views/SettingsView.vue
git commit -m "feat: i18n SettingsView, fix hardcoded de-DE locale"
```

---

## Task 10: i18n Sites Components

**Files:**
- Modify: `assets/js/views/SiteManager.vue`
- Modify: `assets/js/views/SiteOverview.vue`
- Modify: `assets/js/components/SiteCredentialsModal.vue`

- [ ] **Step 1: Update SiteManager.vue template**

| Old | New |
|---|---|
| `<h2 class="mb-4">Meine Standorte</h2>` | `<h2 class="mb-4">{{ $t('hub.sites.title') }}</h2>` |
| `Auswählen` | `{{ $t('hub.sites.select') }}` |
| `<span v-else class="badge bg-primary me-2">Aktiv</span>` | `<span v-else class="badge bg-primary me-2">{{ $t('hub.sites.active') }}</span>` |
| `Löschen` | `{{ $t('hub.sites.delete') }}` |
| `<h5 class="card-title">Neuen Standort hinzufügen</h5>` | `<h5 class="card-title">{{ $t('hub.sites.addSite') }}</h5>` |
| `placeholder="Name (z.B. Ferienhaus)"` | `:placeholder="$t('hub.sites.namePlaceholder')"` |
| `{{ creating ? '...' : 'Hinzufügen' }}` | `{{ creating ? $t('hub.sites.adding') : $t('hub.sites.add') }}` |
| `Füge diese Zeilen in deine evcc.yaml ein:` | `{{ $t('hub.sites.mqttConfigHint') }}` |
| `{{ copied ? 'Kopiert!' : 'Kopieren' }}` | `{{ copied ? $t('hub.sites.copied') : $t('hub.sites.copy') }}` |
| `Zurück zum Dashboard` | `{{ $t('hub.sites.backToDashboard') }}` |

For the site-created message, replace:
```html
<strong>{{ createdSite.name }}</strong> wurde erstellt.
```
With:
```html
<strong>{{ createdSite.name }}</strong> {{ $t('hub.sites.siteCreatedSuffix') }}
```

- [ ] **Step 2: Update SiteManager.vue script error messages**

| Old | New |
|---|---|
| `this.error = 'Standorte konnten nicht geladen werden.'` | `this.error = this.$t('hub.sites.loadError')` |
| `this.error = 'Standort konnte nicht erstellt werden.'` | `this.error = this.$t('hub.sites.createError')` |
| `this.error = 'Standort konnte nicht gelöscht werden.'` | `this.error = this.$t('hub.sites.deleteError')` |

- [ ] **Step 3: Update SiteOverview.vue template**

| Old | New |
|---|---|
| `<h2 class="mb-4">Meine Standorte</h2>` | `<h2 class="mb-4">{{ $t('hub.sites.title') }}</h2>` |
| `Anzeigen` (button text) | `{{ $t('hub.sites.view') }}` |
| `:aria-label="\`${site.name} anzeigen\`"` | `:aria-label="\`${site.name} ${$t('hub.sites.view')}\`"` |
| `Aktiv` (card-footer text) | `{{ $t('hub.sites.active') }}` |
| `Standorte verwalten` | `{{ $t('hub.sites.manageSites') }}` |

Leave `MQTT` button text as-is (it is a technical term, not user-facing text).

- [ ] **Step 4: Update SiteCredentialsModal.vue template**

| Old | New |
|---|---|
| `MQTT-Zugangsdaten — {{ siteName }}` (modal title) | `{{ $t('hub.sites.credentials.title', { siteName }) }}` |
| `aria-label="Schließen"` (btn-close) | `aria-label="Close"` |
| `<span class="visually-hidden">Lädt…</span>` | `<span class="visually-hidden">{{ $t('hub.sites.credentials.loading') }}</span>` |
| `Diese Daten brauchst du für die evcc-Konfiguration dieser Site.` | `{{ $t('hub.sites.credentials.description') }}` |
| `Broker URL` (label) | `{{ $t('hub.sites.credentials.brokerUrl') }}` |
| `Broker Port` (label) | `{{ $t('hub.sites.credentials.brokerPort') }}` |
| `MQTT-Benutzername` (label) | `{{ $t('hub.sites.credentials.mqttUsername') }}` |
| `MQTT-Passwort` (label) | `{{ $t('hub.sites.credentials.mqttPassword') }}` |
| `Topic Prefix` (label) | `{{ $t('hub.sites.credentials.topicPrefix') }}` |
| `{{ copied === 'x' ? 'Kopiert!' : 'Kopieren' }}` (all copy buttons) | `{{ copied === 'x' ? $t('hub.sites.credentials.copied') : $t('hub.sites.credentials.copy') }}` |
| `{{ showPassword ? 'Verbergen' : 'Anzeigen' }}` | `{{ showPassword ? $t('hub.sites.credentials.hide') : $t('hub.sites.credentials.show') }}` |
| `Schließen` (footer close button) | `{{ $t('hub.sites.credentials.close') }}` |

- [ ] **Step 5: Update SiteCredentialsModal.vue script**

Replace:
```js
if (!resp.ok) throw new Error('Zugangsdaten konnten nicht geladen werden.')
```
With:
```js
if (!resp.ok) throw new Error(this.$t('hub.sites.credentials.loadError'))
```

Replace (in catch):
```js
this.error = err.message || 'Zugangsdaten konnten nicht geladen werden.'
```
With:
```js
this.error = err.message || this.$t('hub.sites.credentials.loadError')
```

- [ ] **Step 6: Run tests**

```bash
npm run test -- --run && npm run lint:tsc && npm run lint:i18n
```
Expected: all tests pass, all lint checks pass

- [ ] **Step 7: Commit**

```bash
git add assets/js/views/SiteManager.vue assets/js/views/SiteOverview.vue assets/js/components/SiteCredentialsModal.vue
git commit -m "feat: i18n sites components"
```

---

## Task 11: i18n Legal Pages

**Files:**
- Modify: `assets/js/views/Datenschutz.vue`
- Modify: `assets/js/views/Impressum.vue`
- Modify: `assets/js/views/Nutzungsbedingungen.vue`

These pages currently use `ref('<p class="text-muted">Lädt...</p>')` with `v-html`, which prevents using `$t()`. Refactor to use `data()` with a `loading` flag and conditional template rendering.

- [ ] **Step 1: Rewrite Datenschutz.vue**

```vue
<template>
  <div class="container py-4">
    <p v-if="loading" class="text-muted">{{ $t('hub.legal.loading') }}</p>
    <div v-else v-html="content" />
  </div>
</template>

<script lang="ts">
import { defineComponent } from "vue";
export default defineComponent({
  name: "Datenschutz",
  data() {
    return {
      loading: true,
      content: '',
    };
  },
  async mounted() {
    try {
      const res = await fetch("/legal/datenschutz.html");
      if (res.ok) this.content = await res.text();
    } catch {
      // content stays empty
    } finally {
      this.loading = false;
    }
  },
});
</script>
```

- [ ] **Step 2: Rewrite Impressum.vue**

Same pattern as Datenschutz.vue but:
- `name: "Impressum"`
- fetch URL: `/legal/impressum.html`

- [ ] **Step 3: Rewrite Nutzungsbedingungen.vue**

Same pattern as Datenschutz.vue but:
- `name: "Nutzungsbedingungen"`
- fetch URL: `/legal/nutzungsbedingungen.html`

- [ ] **Step 4: Run tests**

```bash
npm run test -- --run && npm run lint:tsc
```
Expected: all tests pass

- [ ] **Step 5: Commit**

```bash
git add assets/js/views/Datenschutz.vue assets/js/views/Impressum.vue assets/js/views/Nutzungsbedingungen.vue
git commit -m "feat: i18n legal pages, refactor loading state to template"
```

---

## Task 12: i18n Dev Pages and Energyflow

**Files:**
- Modify: `assets/js/views/Optimize.vue`
- Modify: `assets/js/views/Energy.vue`
- Modify: `assets/js/views/Issue.vue`
- Modify: `assets/js/components/Optimize/BatteryConfigurationTable.vue`
- Modify: `assets/js/components/Energyflow/Visualization.vue`

> Before implementing, fully read BatteryConfigurationTable.vue and Issue.vue to confirm all hardcoded string locations.

- [ ] **Step 1: Update Optimize.vue**

Replace all strings with `$t('hub.debug.optimize.*')` keys per the mapping in Task 4 (title, description, section headings "Result: Charging Plan", "saved", "Result: SoC Projection", "Input: Grid Prices", "Input: Battery", "charge efficiency", "discharge efficiency", "Time Series", "Raw Data", "Request:", "Response:", "nothing to see here").

The `title` prop of `<TopHeader>` uses a string: change to `:title="$t('hub.debug.optimize.title')"`.

- [ ] **Step 2: Update Energy.vue**

Replace "Energy Overview" (TopHeader title), "This page is work in progress.", "nothing to see here" with `$t('hub.debug.energy.*')` keys.

- [ ] **Step 3: Update Issue.vue**

Replace "Please write your issue in English so everyone can participate.", form `placeholder` values ("Brief description of the problem"), and error messages with `$t('hub.issue.*')` keys.

- [ ] **Step 4: Update BatteryConfigurationTable.vue**

Replace all table headers (Battery, State of Charge, SoC Range, Energy Value, Power Range, Max Discharge, Grid Interaction, Demand Profile, SoC Goals) and value labels (Charge / Discharge, Charge, Discharge, None, steps, goals) with `$t('hub.debug.battery.*')` keys.

- [ ] **Step 5: Update Visualization.vue**

Replace:
- `<div class="label-scale-name">In</div>` with `<div class="label-scale-name">{{ $t('hub.energyflow.in') }}</div>`
- Find the corresponding `Out` label and replace with `{{ $t('hub.energyflow.out') }}`

- [ ] **Step 6: Run full lint suite**

```bash
npm run test -- --run && npm run lint:tsc && npm run lint:i18n
```
Expected: all tests pass, all lint checks pass

- [ ] **Step 7: Commit**

```bash
git add assets/js/views/Optimize.vue assets/js/views/Energy.vue assets/js/views/Issue.vue assets/js/components/Optimize/BatteryConfigurationTable.vue assets/js/components/Energyflow/Visualization.vue
git commit -m "feat: i18n dev pages and Energyflow visualization"
```
